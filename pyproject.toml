[project]
name = "renovate-uv-workspaces"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy",
]

[tool.uv.workspace]
members = [
    "subproject",
]
